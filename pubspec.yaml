name: my_flutter_app
description: A new Flutter project.

publish_to: 'none' # If this is a private project, leave it as 'none'

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  chewie: ^1.4.2
  video_player: ^2.5.1
  english_words: ^4.0.0
  provider: ^6.1.2
  geolocator: ^13.0.2
  firebase_core: ^3.10.0  # Ensure this is compatible with your other packages
  firebase_analytics: ^11.4.0  # Updated to a compatible version
  cloud_firestore: ^5.4.0  # Or another version as needed
  firebase_database: ^11.3.0
  email_validator: ^3.0.0
  url_launcher: ^6.0.0
  mailer: ^6.4.1
  flutter_animate: ^4.0.3
  firebase_auth: ^5.5.1
  google_sign_in: ^6.2.1
  marquee: ^2.2.3
  fl_chart: ^0.70.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  firebase_data_connect: ^0.1.2+6

flutter:
  uses-material-design: true
  assets:
    - assets/0_Earth_Planet_3840x2160.mp4
