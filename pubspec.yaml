name: my_flutter_app
description: A new Flutter project.

publish_to: 'none' # If this is a private project, leave it as 'none'

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  video_player: ^2.5.1
  english_words: ^4.0.0
  provider: ^6.0.0
  geolocator: ^9.0.1
  firebase_core: ^3.4.0  # Ensure this is compatible with your other packages
  firebase_analytics: ^11.4.0  # Updated to a compatible version
  cloud_firestore: ^5.4.0  # Or another version as needed



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  firebase_data_connect: ^0.1.2+6

flutter:
  uses-material-design: true
  assets:
    - assets/0_Earth_Planet_3840x2160.mp4
